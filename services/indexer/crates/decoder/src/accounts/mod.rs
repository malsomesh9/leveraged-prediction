//! This code was AUTOGENERATED using the Codama library.
use crate::LeveragedPredictionDecoder;
use crate::PROGRAM_ID;

pub mod market;
pub mod protocol_config;
pub mod user_liquidity;
pub mod user_positions;

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[cfg_attr(feature = "serde", serde(tag = "type", content = "data"))]
pub enum LeveragedPredictionAccount {
    Market(Box<market::Market>),
    ProtocolConfig(Box<protocol_config::ProtocolConfig>),
    UserLiquidity(Box<user_liquidity::UserLiquidity>),
    UserPositions(Box<user_positions::UserPositions>),
}

impl<'a> carbon_core::account::AccountDecoder<'a> for LeveragedPredictionDecoder {
    type AccountType = LeveragedPredictionAccount;

    fn decode_account(
        &self,
        account: &'a solana_account::Account,
    ) -> Option<carbon_core::account::DecodedAccount<Self::AccountType>> {
        if account.owner != PROGRAM_ID {
            return None;
        }

        let data = account.data.as_slice();

        {
            if let Some(decoded) = market::Market::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: LeveragedPredictionAccount::Market(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = protocol_config::ProtocolConfig::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: LeveragedPredictionAccount::ProtocolConfig(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = user_liquidity::UserLiquidity::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: LeveragedPredictionAccount::UserLiquidity(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }
        {
            if let Some(decoded) = user_positions::UserPositions::decode(data) {
                return Some(carbon_core::account::DecodedAccount {
                    lamports: account.lamports,
                    data: LeveragedPredictionAccount::UserPositions(Box::new(decoded)),
                    owner: account.owner,
                    executable: account.executable,
                    rent_epoch: account.rent_epoch,
                });
            }
        }

        None
    }
}
