//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct DelegateMarket {
    pub market_id: u16,
    pub validator: Pubkey,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct DelegateMarketInstructionAccounts {
    pub payer: solana_pubkey::Pubkey,
    pub protocol_config: solana_pubkey::Pubkey,
    pub buffer_market: solana_pubkey::Pubkey,
    pub delegation_record_market: solana_pubkey::Pubkey,
    pub delegation_metadata_market: solana_pubkey::Pubkey,
    pub market: solana_pubkey::Pubkey,
    pub owner_program: solana_pubkey::Pubkey,
    pub delegation_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl DelegateMarket {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[223, 125, 110, 148, 99, 176, 143, 72] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for DelegateMarket {
    type ArrangedAccounts = DelegateMarketInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let payer = next_account(&mut iter)?;
        let protocol_config = next_account(&mut iter)?;
        let buffer_market = next_account(&mut iter)?;
        let delegation_record_market = next_account(&mut iter)?;
        let delegation_metadata_market = next_account(&mut iter)?;
        let market = next_account(&mut iter)?;
        let owner_program = next_account(&mut iter)?;
        let delegation_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(DelegateMarketInstructionAccounts {
            payer,
            protocol_config,
            buffer_market,
            delegation_record_market,
            delegation_metadata_market,
            market,
            owner_program,
            delegation_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
