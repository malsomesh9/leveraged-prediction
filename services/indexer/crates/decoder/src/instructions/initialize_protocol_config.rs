//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct InitializeProtocolConfig {}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct InitializeProtocolConfigInstructionAccounts {
    pub admin: solana_pubkey::Pubkey,
    pub fee_authority: solana_pubkey::Pubkey,
    pub program: solana_pubkey::Pubkey,
    pub program_data: solana_pubkey::Pubkey,
    pub collateral_mint: solana_pubkey::Pubkey,
    pub protocol_config: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl InitializeProtocolConfig {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[28, 50, 43, 233, 244, 98, 123, 118] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for InitializeProtocolConfig {
    type ArrangedAccounts = InitializeProtocolConfigInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let admin = next_account(&mut iter)?;
        let fee_authority = next_account(&mut iter)?;
        let program = next_account(&mut iter)?;
        let program_data = next_account(&mut iter)?;
        let collateral_mint = next_account(&mut iter)?;
        let protocol_config = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(InitializeProtocolConfigInstructionAccounts {
            admin,
            fee_authority,
            program,
            program_data,
            collateral_mint,
            protocol_config,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
