//! This code was AUTOGENERATED using the Codama library.
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;
use solana_pubkey::Pubkey;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct DelegateUserPositions {
    pub validator: Pubkey,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct DelegateUserPositionsInstructionAccounts {
    pub user: solana_pubkey::Pubkey,
    pub buffer_user_positions: solana_pubkey::Pubkey,
    pub delegation_record_user_positions: solana_pubkey::Pubkey,
    pub delegation_metadata_user_positions: solana_pubkey::Pubkey,
    pub user_positions: solana_pubkey::Pubkey,
    pub owner_program: solana_pubkey::Pubkey,
    pub delegation_program: solana_pubkey::Pubkey,
    pub system_program: solana_pubkey::Pubkey,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl DelegateUserPositions {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[147, 104, 221, 210, 31, 52, 34, 53] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for DelegateUserPositions {
    type ArrangedAccounts = DelegateUserPositionsInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let user = next_account(&mut iter)?;
        let buffer_user_positions = next_account(&mut iter)?;
        let delegation_record_user_positions = next_account(&mut iter)?;
        let delegation_metadata_user_positions = next_account(&mut iter)?;
        let user_positions = next_account(&mut iter)?;
        let owner_program = next_account(&mut iter)?;
        let delegation_program = next_account(&mut iter)?;
        let system_program = next_account(&mut iter)?;

        let remaining = iter.as_slice();

        Some(DelegateUserPositionsInstructionAccounts {
            user,
            buffer_user_positions,
            delegation_record_user_positions,
            delegation_metadata_user_positions,
            user_positions,
            owner_program,
            delegation_program,
            system_program,
            remaining: remaining.to_vec(),
        })
    }
}
