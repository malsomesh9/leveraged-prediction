//! This code was AUTOGENERATED using the Codama library.
use crate::types::Direction;
use carbon_core::account_utils::next_account;
use carbon_core::borsh;
use carbon_core::deserialize::ArrangeAccounts;
use carbon_core::deserialize::CarbonDeserialize;
use carbon_core::CarbonDeserialize;

#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
#[derive(Debug, Clone, borsh::BorshSerialize, CarbonDeserialize, PartialEq)]
pub struct OpenPosition {
    pub nonce: u32,
    pub task_salt: [u8; 32],
    pub direction: Direction,
    pub collateral: u64,
    pub min_entry_price: i64,
    pub max_entry_price: i64,
}

#[derive(Debug, Clone, PartialEq)]
#[cfg_attr(feature = "serde", derive(serde::Serialize, serde::Deserialize))]
pub struct OpenPositionInstructionAccounts {
    pub user: solana_pubkey::Pubkey,
    pub session_signer: solana_pubkey::Pubkey,
    pub protocol_config: solana_pubkey::Pubkey,
    pub market: solana_pubkey::Pubkey,
    pub user_positions: solana_pubkey::Pubkey,
    pub pool_token_account: solana_pubkey::Pubkey,
    pub derived_fee_authority: solana_pubkey::Pubkey,
    pub fee_token_account: solana_pubkey::Pubkey,
    pub user_token_account: solana_pubkey::Pubkey,
    pub payout_escrow_token_account: solana_pubkey::Pubkey,
    pub collateral_mint: solana_pubkey::Pubkey,
    pub price_update: solana_pubkey::Pubkey,
    pub token_program: solana_pubkey::Pubkey,
    pub crank_signer: solana_pubkey::Pubkey,
    pub magic_program: solana_pubkey::Pubkey,
    pub session_token: Option<solana_pubkey::Pubkey>,
    pub remaining: Vec<solana_instruction::AccountMeta>,
}

impl OpenPosition {
    pub fn decode(data: &[u8]) -> Option<Self> {
        if data.len() < 8 {
            return None;
        }
        let discriminator = &data[0..8];
        if discriminator != &[135, 128, 47, 77, 15, 152, 240, 49] {
            return None;
        }

        let data_slice = data;

        let data_slice = &data_slice[8..];

        Self::deserialize(data_slice)
    }
}

impl ArrangeAccounts for OpenPosition {
    type ArrangedAccounts = OpenPositionInstructionAccounts;

    fn arrange_accounts(
        accounts: &[solana_instruction::AccountMeta],
    ) -> Option<Self::ArrangedAccounts> {
        let mut iter = accounts.iter();

        let user = next_account(&mut iter)?;
        let session_signer = next_account(&mut iter)?;
        let protocol_config = next_account(&mut iter)?;
        let market = next_account(&mut iter)?;
        let user_positions = next_account(&mut iter)?;
        let pool_token_account = next_account(&mut iter)?;
        let derived_fee_authority = next_account(&mut iter)?;
        let fee_token_account = next_account(&mut iter)?;
        let user_token_account = next_account(&mut iter)?;
        let payout_escrow_token_account = next_account(&mut iter)?;
        let collateral_mint = next_account(&mut iter)?;
        let price_update = next_account(&mut iter)?;
        let token_program = next_account(&mut iter)?;
        let crank_signer = next_account(&mut iter)?;
        let magic_program = next_account(&mut iter)?;
        let session_token = next_account(&mut iter);

        let remaining = iter.as_slice();

        Some(OpenPositionInstructionAccounts {
            user,
            session_signer,
            protocol_config,
            market,
            user_positions,
            pool_token_account,
            derived_fee_authority,
            fee_token_account,
            user_token_account,
            payout_escrow_token_account,
            collateral_mint,
            price_update,
            token_program,
            crank_signer,
            magic_program,
            session_token,
            remaining: remaining.to_vec(),
        })
    }
}
